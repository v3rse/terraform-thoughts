{
  "name": "webhook-handler",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "infra:deploy": "npm run build && cd infra && terraform apply",
    "infra:destroy": "cd infra && terraform destroy",
    "infra:init": "cd infra && terraform init",
    "build": "npm run lint && cd src  && zip -r ../dist/webhook-handler.zip .",
    "lint": "standard src/**/*.js",
    "lint:fix": "npm run lint -- --fix"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "standard": "14.3.4"
  }
}